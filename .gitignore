node_modules/
.next/
out/
build/
next-env.d.ts
*.tsbuildinfo

# 사용자 데이터 — 커밋 금지 (NFR-ENTRY-05: 기기 안에만 존재한다)
# 이 폴더는 첫 실행 때 코드가 만든다(src/lib/db/index.ts). 저장소에 둘 필요가 없다.
data/

# 비밀 — 커밋 금지.
# `.env` 로 시작하는 것은 전부 막고 본보기 파일만 예외로 연다.
# 이름을 하나씩 적으면 .env.production 같은 새 파일이 그물을 빠져나간다.
.env*
!.env.example

# 시험 산출물
test/.out/

# 문서 작성 지침 — 체인 밖 참고물이라 저장소에 두지 않는다.
docs/guideline/
